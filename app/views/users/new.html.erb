<h1>New User</h1>

<% if current_user %>
  <%= "Welcome, #{current_user.name}" %>
  <%= link_to 'Logout', session_path(current_user.id), method: 'delete' %>
<% else %>
  <%= render 'form' %>
<% end %>